#ifndef __FUNCS_ND__
#define __FUNCS_ND__

// n-D functions

template <typename T> T schwefel(T* arr, int dims);
template <typename T> T rosenbrock(T* arr, int dims);
template <typename T> T levy(T* arr, int dims);

#endif
